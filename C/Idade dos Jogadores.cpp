#include <stdio.h>
#include <locale.h>

int main() {

	setlocale(LC_ALL, "Portuguese");
	
	float idade,resultado;
	
	
	printf("Por favor, sua idade: ");
	scanf("%f", &idade);
	
	if(idade>=18)
		printf("Você vai para o Senior ")
	{
		total = copias*0.20;
	}
	else
	{
		total = copias*0.25;
	};
	printf("O Total da impressão foi R$%.2f\n", total);
};
