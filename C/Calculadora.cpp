#include <stdio.h>
#include <locale.h>

int main() {
	
	setlocale(LC_ALL, "Portuguese")

	char formula;
	float numA,numB
	
	printf("Cite qual fórmula você quer usar, sendo elas +,-,* e /: ")
	scanf ("%f", &formula);
	
	printf("Cite o primeiro número: ")
	scanf ("%f", &numA);
	
		printf("Cite o segundo número: ")
	scanf ("%f", &numB);
	
	switch
	};
